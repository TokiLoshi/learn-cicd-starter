name: ci

on:
  pull_request:
    branches: [main]

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Test go
        uses: actions/checkout@v5
        run: go test ./...

      - name: Echo Go version
        run: (exit 0)
